// Shared animation values — keep motion consistent across the site.

/** Signature luxury easing curve used for all editorial transitions. */
export const EASE_LUXE = [0.22, 1, 0.36, 1] as const;

/** Default hero slide rotation interval (ms). */
export const HERO_SLIDE_MS = 6500;
